import 'package:flutter/material.dart';

class PasswordRegisterScreen extends StatelessWidget {
  static const routeName = '/password-register';

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Password register screen"),
    );
  }
}
