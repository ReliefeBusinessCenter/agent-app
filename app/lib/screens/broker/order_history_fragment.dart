import 'package:flutter/material.dart';

class BrokerOrderHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      color: Theme.of(context).accentColor,
      child: Center(
        child: Text("Deals History"),
      ),
    );
  }
}
